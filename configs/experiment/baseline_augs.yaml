# @package _global_

defaults:
  - _self_
  - baseline_with_losses
  - override /data/transforms@data.train_transforms: augs

tags: ["baseline", "augs"]

seed: 12345

logger:
  wandb:
    tags: ${tags}