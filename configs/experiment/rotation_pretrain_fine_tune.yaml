# @package _global_

defaults:
  - _self_
  - baseline_augs

tags: ["rotation_pretrain", "augs"]

seed: 12345

ckpt_path: logs/train/runs/rotation_pretrain_augs/checkpoints/epoch_074.ckpt
ignored_children:
  - fc

logger:
  wandb:
    tags: ${tags}